#ifndef SSD_h
#define SSD_h

#include "Gpio.h"

void init(void);
void display(u8 number);
void select(u8 digit);

#endif
/*
#ifndef SSD_h
#define SSD_h
#include "Gpio.h"

void init(void);
void diplay(u8 number);
void select(u8 digit);

#endif */